import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(
    <App />,
  document.getElementById('root')//id는 문서 내에서 유일..id속성이 root인 요소를 찾고, 이를 나타내는 element 반환
);


